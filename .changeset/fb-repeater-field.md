---
'@10x-media/form-builder': minor
---

Add `repeater` field type: a field that captures a list of identical rows, each containing a set of sub-fields defined once in the admin UI. Includes row-count validation (minRows/maxRows), a client-side renderer with add/remove row controls, per-row sub-field validation on the server, and a numbered row view in the submission answers panel.
