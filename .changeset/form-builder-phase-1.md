---
"@10x-media/form-builder": minor
---

Add the field spine: the `defineFormField` primitive with a core field set (text, textarea, email, number, select, checkbox), a `false | true | object` field-type registry, form authoring via native blocks, typed and self-describing localized submissions validated server-side, and a formatted admin answers view.
