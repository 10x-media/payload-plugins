---
'@10x-media/sipgate': patch
---

Fix RTCM transfer payload, wire webhookUrl into live-call XML callbacks, align OAuth/IVR README with webhookUrl vs serverURL, add collection access defaults, OAuth nonce TTL, Neo-only call history with limit forwarding, and remove dead probeDevices/query-string code. Add IVR override keys and SipgateOnInitSlugs for custom collection slugs.
