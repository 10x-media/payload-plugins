---
"@10x-media/form-builder": minor
---

Add a native, Payload-style condition builder for `visibleWhen`/`validateWhen`: editors build conditional visibility and conditional validation visually (field, operator, value -- the same look as Payload's list filters), stored as a canonical `Where` and enforced server-side. Conditions are normalized and validated on save.
