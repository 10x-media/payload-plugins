---
'@10x-media/form-builder': minor
---

Presentation seam (Phase 6b): serializable `PresentationDescriptor` + `false/true/object` descriptor registry; editor-chosen `defaultPresentation` on the forms collection; `<Form>` presentation resolution with render-time override via the `presentation` prop; built-in page, modal, drawer, and inline presentations; composable dependency-free accessible overlay primitives (`DialogSurface`, `Backdrop`, `useFocusTrap`, `useScrollLock`, `useDismiss`); layered and non-breaking.
