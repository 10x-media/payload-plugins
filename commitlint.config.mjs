// Conventional Commits, enforced by the husky commit-msg hook.
// Scopes are free-form; prefer the package short name (jobs, automations, webhooks,
// harness, docs, repo). See CONTRIBUTING.md for the convention.
export default {
	extends: ['@commitlint/config-conventional'],
}
