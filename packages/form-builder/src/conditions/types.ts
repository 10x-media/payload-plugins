import type { Operator, Where } from 'payload'

export type { Operator, Where }

/** A serializable field-visibility/validation condition, stored in Payload's canonical `Where` shape. */
export type FieldCondition = Where
