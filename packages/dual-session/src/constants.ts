/** Request header carrying the resolved {@link import('./types').AuthScope} for a request. */
export const AUTH_SCOPE_HEADER = 'x-payload-auth-scope'

/** This plugin's registered slug, used to find its options on a booted config. */
export const PLUGIN_SLUG = '@10x-media/dual-session'
