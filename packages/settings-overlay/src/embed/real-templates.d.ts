/**
 * The real `@payloadcms/next/templates`, reached under a name the bundler aliases straight to
 * the file, past the package's export map. Types are the package's own.
 */
declare module 'payload-next-templates-real' {
	export * from '@payloadcms/next/templates'
}
